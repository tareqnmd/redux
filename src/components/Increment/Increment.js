import React from 'react';
import { IncrementButton } from '../../styles/Countdown';

const Increment = () => {
    return (
        <IncrementButton>
            Increment
        </IncrementButton>
    );
};

export default Increment;