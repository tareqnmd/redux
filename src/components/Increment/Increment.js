import React from 'react';
import { IncrementButton } from '../../styles/Countdown';

const Increment = () => {
    return (
        <IncrementButton>
            Increment by 1
        </IncrementButton>
    );
};

export default Increment;